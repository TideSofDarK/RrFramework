* Critical

- [X] Synchronization (?) bug with host written buffers.
  - Turned out to be an issue of writing to host visible buffers before waiting on the fence associated with current frame.
  - Solution: call Iterate() after waiting on the fence.
- [X] Add deletion queues for Vulkan objects.
- [X] Things such as rendering to cubemap requires more advanced image view handling.
  - A hive of wrapped image views + hash?
  - Solution: implemented custom storage for views that combines a hive and a map.
  - Any image view gets created on demand unless it's already cached in storage.
- [X] Hash collision handling across all usages of Rr_Map.
- [X] Hashing objects such as VkFramebuffer looks very bad.
- [X] Simplify pipeline layout creation.
  - In the end it had to be more complex though.
- [X] Inter-node sync, e.g., sequential compute dispatches writing to the same resource.
  - Added RW versions of Rr_BindStorageXXX commands for better synchronization opportunities.
  - Added Rr_ComputeBarrier() for compute nodes.
  - Since you can't put barriers amidst render passes, it's user responsibility to avoid undefined behavior.
- [X] Remove Rr_LoadingThread.
  - Remove Rr_UploadImageXXX stuff which is going to be replaced by graph nodes.
  - Current plan is to decouple Rr_Graph from Rr_Frame and make it possible to create subgraphs (possibly from other threads).
  - For now, make transfer-only graphs that can be submitted from secondary threads.
- [X] Finalize said decoupling:
  - Track graph capabilities.
  - Move arena and command pools to thread context.
- [X] Should referenced pipelines and samplers be hashed to optimize decrement references pass? 
  - Solution: put handles to hash set.
- [X] Binding arrays support.
- [X] 3D image support.
- [X] Image array support.
- [X] Refactor duplicated code in Rr_ExecuteComputeNode and Rr_ExecuteGraphicsNode functions.
- [X] MSAA support.
- [X] Check if descriptor set layouts are properly hashed.
- [X] Refactor Rr_AddXXXNode functions to ensure better type safety.
- [X] See if it's possible to get rid of descriptor allocator arenas.
- [X] Make locking more granular. gRHI->Lock should only be locked when allocating.
- [X] Framebuffers/SyncStates require different hash table implementation. Current implementation is a mess...
- [X] Query platform mouse position before polling.
- [X] Remove GLTF API.
- [X] Make threads claim arenas from a free list.
- [X] Does PushXXXIntoHive need to return iterator?
- [X] Releasing same resource (buffer, image, etc) twice leads to annoying bugs.
- [X] Replace VMA with custom allocator.
- [X] Support linear tiling (?) formats such as R8G8B8.
  - Linear tiling appears useless. Added more common format support instead.
- [X] Public API should use const where applicable.
- [X] Vulkan: flexible image subresource layouts inside compute nodes.
- [X] Error handling: RHI resource creation.
- [ ] Reassess usage of assert() across the library code.
- [ ] Extend Rr_Config.
  - [ ] More window options.
  - [ ] Arena reserve/commit size.
  - [ ] GPU allocator chunk size.
- [ ] Documentation.
- [ ] Replace hash trie usage with Rr_HashTable.h where applicable.
- [ ] Rr_ReleaseXXX functions can validate input pointer via getting hive iterator.

* Features

** User Interface

- [X] Many newlines and scroll in text input bug.
- [X] Shift-Enter doesn't work.
- [X] Enter at cursor 0 doesn't work.
- [X] Escape to close popup windows.
- [X] Allow having scroll area width larger than line height.
- [X] Separate colors for comboboxes.
- [X] Separate colors for radio buttons.
- [X] Hex color copy/paste.
- [X] Backspace in input fields.
- [X] Separate window/contens padding.
- [X] Auto resize ignores title width.
- [X] Input fields with custom clip rects happen to ignore parent bounds.
- [X] Better tabs.
- [X] Upgrade click system to allow double clicking titles.
  - Also fix input field flicker.
- [X] Add separate ID/Hash stack.
- [X] Fix broken clip rects.
- [X] Apply scalar changes immediately if possible.
- [X] Make drag behavior properly handle buttons and support double clicks.
- [X] Support for linear color space swapchains.
  - Put colors into an array for easier conversion. 
- [X] Draggable scalars.
- [X] Clamps/ratios for draggable scalars.
- [X] Combobox.
- [X] Auto-resizing windows.
- [X] Sliders.
- [X] Keyboard input and related widgets.
- [X] Color picker.
- [X] Radio buttons.
- [X] Nested windows.
- [X] Swap font rendering with stb_truetype.
- [X] Horizontal scrollbar.
- [X] Child window borders ignore mouse wheel events.
- [X] Auto resize should reset scroll.
  - For now, resetting scroll value when FillRatio >= 1.0f.
- [X] Input float/double: dragging only changes the integer part.
- [X] Input field: reset focus when clicking elsewhere.
- [X] Input field: CTRL+BACKSCAPE to remove words.
- [X] Input field: DELETE key behavior.
- [ ] Tables.
- [ ] Closing popups doesn't restore window focus.
- [ ] Make sure widgets don't look broken with extremely small window sizes.
- [ ] Proper stack data structure.
- [ ] Some options for horizontal child layouts + asserts.
- [ ] Flickering:
  - [ ] Uncollapsing a child window. Parent fails to adjust its size immediately.
  - [ ] Uncollapsing something for the first time. Flexible widgets have no idea how much width is available.
    - Seems unfixable without deferring?
  - [ ] Scrollbar hide/show lags one frame behind.
  - [ ] Tabs flicker 2 or 3 times depending on the order.
    - Probably tackle this after fixing the rest of the flickering.
  - In IMGUI a widgets layout can't depend on any subsequent widgets layout. 
  - Unfortunately, the way to go is to scrap flexible widget stuff and rely on manually provided widths for alignment.
  - Wrapped text already does that.

** Debug

- [X] Vulkan markers.
- [ ] Very basic profilers for CPU/GPU.
- [ ] Memory arena registry.
- [ ] Allow inserting debug markers not associated with nodes.
- [ ] Separate define for Vulkan markers.

** Examples

- [-] Modern Rendering
  - [X] Implement PBR.
  - [ ] Improve SSAO.
  - [ ] Fix or replace MSAA.
- [ ] Box3D Integration
- [X] Pre-rendered Depth
- [ ] Particles
- [ ] Light Space Perspective Shadow Maps
- [X] Mip Generation
- [ ] Slang Integration
- [ ] Alpha-to-Coverage

** Low-priority

- [X] Move to GLFW.
- [X] Move to C11.
- [X] Move back to C99!
- [X] Remove InputMap.
- [X] Decide where it makes sense to use size_t instead of uint32_t. Also VK_DEVICE_SIZE etc.
  - Solution: use size_t for user-provided counts (i.e. number of color targets).
  - Otherwise stick to types that correspond to underlying Vulkan structs.
- [X] With GLFW swapchain gets recreated immediately after launch.
- [X] Own implementation of Windows/Linux platform layers.
- [X] Make Rr_HashMap and Rr_Hive follow somewhat similar conventions.
- [X] Rr_IntVec* operators.
- [ ] Vulkan: memory defragmentation.
- [ ] Vulkan: combine image views if format supports both STORAGE and SAMPLED.
- [ ] Add support for uniform texel buffers.
- [ ] Convert Rr_HashTrie into inline header-based collection.
- [ ] Convert RR_ARRAY into inline header-based collection.
- [ ] Header-based stuff should probably be internal.
  - The goal is API that's 100% accessible from other languages.
  - Nothing can be done about Rr_Math.h though?
- [ ] Further RHI decoupling.
- [ ] Support for immutable buffers/images that don't require synchronization.
- [ ] Controller support.
